#Tehtava1print ("Hello World!")#Tehtava2a = input ('Anna numero 1: ')b = input ("Anna numero 2: ")if a > b:    print (a + ' on suurempi')elif a < b:    print (b + ' on pienempi')else:    print ("Yhtäsuuret!")#Tehtava3import randoma = random.randint(0,99)b = random.randint(0,99)if a > b:    print ('a on suurempi')elif a < b:    print ('b on suurempi')else:    print ("Yhtäsuuret!")   #Tehtävä4import randoma = random.randint(0,9)b = random.randint(0,9)def summa(a,b):    return a + bprint(f'Lukujen {a} ja {b} summa on: {summa(a,b)}')#Tehtävä5import randomoikein = 0lst = []    for i in range(0,5):    a = random.randint(0,10)    b = random.randint(0,10)    print(f'{a} * {b} = ')    vastaus = int(input())    if vastaus == a*b:        lst.append(f'Oikein :-) {a} * {b} = {vastaus}')        oikein += 1    else:        lst.append(f'Väärin :-( Oikea vastaus on: {a} * {b} = {a*b}')        print("\n",lst[0], "\n",lst[1], "\n",lst[2],"\n",lst[3],"\n",lst[4])print(f'\n Sait {oikein} oikein!')#Tehtävä6class Murtoluku:    def __init__(self, os, nim):        self.os = os        self.nim = nim            def tulosta(self):        print (f'{self.os} / {self.nim}')            def sievenna(self):        syt = self.SYT()        self.os = int(self.os / syt)        self.nim = int(self.nim / syt)            def SYT(self):      #suurin yhteinen tekijä        a = self.os        b = self.nim        while b != 0:            t = b            b = a % b            a = t        return aml = Murtoluku(3 , 9)ml.tulosta()ml.sievenna()ml.tulosta()       class Fraction:    def __init__(self, nu, de):        self.nu = nu        self.de = de            def tulosta(self):        print (f'{self.nu} / {self.de}')            def sievenna(self):        syt = self.SYT()        self.nu = int(self.nu / syt)        self.de = int(self.de / syt)            def SYT(self):      #suurin yhteinen tekijä        a = self.nu        b = self.de        while b != 0:            t = b            b = a % b            a = t        return aml = Fraction(34562, 311058)ml.tulosta()ml.sievenna()ml.tulosta()                                                                                                                                